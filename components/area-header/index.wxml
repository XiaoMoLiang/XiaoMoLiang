<!--components/area-header/index.wxml-->
<view class="header">
    <view class="title">{{title}}</view>
    <view class="right">
        <view class="slot">
            <slot></slot>
        </view>
        <view class="default" wx:if="{{showRight}}">
            <text>{{rightText}}</text>
            <image class="icon" src="../../images/icon/arrow-right.png"></image>
        </view>
    </view>
</view>